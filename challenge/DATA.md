# Challenge data

`candidates.jsonl` (~465 MB) is **not committed to GitHub** (size limit).

## Local setup

Place the hackathon file at:

```
challenge/candidates.jsonl
```

## Colab

After `git clone`, either:

1. Upload `candidates.jsonl` in the notebook upload cell, or  
2. Copy from Google Drive to `challenge/candidates.jsonl`

Sample profiles for tests: `sample_candidates.json` (included in repo).
